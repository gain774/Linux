--[[
  国庫（設計ドキュメント §9.1〜9.2）

  NPC スプレッドと委託所手数料で徴収した分を、消滅させずに記帳する。
  **プレイヤーが払う額・受け取る額は変わらない。** 変わるのは
  「消えていた」が「行き先が見える」ようになることだけ。
]]
TreasuryConfig = {
    -- 既定 ON。記帳するだけでゲームの挙動は変わらない。
    -- 組合への補助金（§9.4）を作る前に、どれだけ税が集まるのかの実測が取れる。
    enabled = true,

    -- 準備金として保持する月数。月間税収の何か月分か。
    -- これを超えた分が補助金の予算になる（補助金は未実装）。
    reserveMonths = 3.0,

    -- 起動時に sql/schema.sql を流す
    autoMigrate = true,

    label = '$',
}
